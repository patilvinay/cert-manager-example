---
apiVersion: cert-manager.io/v1alpha2
kind: Certificate
metadata:
  name: hello-subdomain-cert-prod
  #  namespace: default
spec:
  secretName: hello-subdomain-cert-prod
  issuerRef:
    name: letsencrypt
    kind: ClusterIssuer
  commonName: hello.ngxconnect.in
  dnsNames:
    - hello.ngxconnect.in
  
---
